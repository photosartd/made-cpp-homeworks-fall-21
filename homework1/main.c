#include <stdio.h>
#include "exchange.h"
#include "in_out_helpers.h"

int main() {
    start_example_static_session();
    //uncomment to start dynamic currency exchange session
    //start_currency_session();
}